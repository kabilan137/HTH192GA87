// Placeholder for annotated diff — kept minimal for MVP
// The issue cards in ReportView already show file+line references.
// A full inline diff view is a nice-to-have beyond MVP scope.
export default function AnnotatedDiff({ diff, issues }) {
  return null;
}
